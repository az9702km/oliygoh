---
const { style, form, classes, disabled, justify } = Astro.props;
---

<button
  class:list={[
    "btn",
    style ? `btn-${style}` : "",
    justify ? `btn-${justify}` : "",
    form ? form : "",
    classes,
  ]}
  disabled={disabled}>
  <slot />
</button>
